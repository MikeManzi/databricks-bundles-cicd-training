# The main pipeline for databricks_bundles_cicd_training

resources:
  pipelines:
    databricks_bundles_cicd_training_etl:
      name: databricks_bundles_cicd_training_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/databricks_bundles_cicd_training_etl"

      libraries:
        - glob:
            include: ../src/databricks_bundles_cicd_training_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
